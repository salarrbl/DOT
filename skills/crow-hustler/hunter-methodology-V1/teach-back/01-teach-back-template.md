# Teach-Back Template

The agent fills this out after every session. Human reads it in under 10 minutes.

---

# [Title: What we did in one sentence]

**Date:** YYYY-MM-DD
**Session Type:** [hunt / learn / research / recon / methodology-update]
**Target (if applicable):** [program name / endpoint]
**Duration:** [how long we worked]

---

##  What We Did Today

[2-4 sentences. Plain language. What did we actually do? No jargon dump.]

Example: "We tested Instagram's OAuth flow for CSRF in the `redirect_uri` parameter. Tried 15 payload variations. Found nothing valid, but discovered that they strip all special chars from the `state` parameter."

---

##  What I (The Agent) Learned

[3-7 bullet points. New knowledge. Written like I'm explaining it to you over coffee.]

- [Single concrete thing I learned]
- [Another thing]
- [Why this matters — context, not just fact]

Example:
- Instagram's OAuth implementation strips special chars from `state`, but NOT from `redirect_uri` — this is unusual. Most platforms strip both.
- When `state` is stripped but `redirect_uri` isn't, the attack vector shifts from CSRF to open redirect + token theft.
- The error responses for invalid redirect_uri contain the full attempted URL in the HTML body (potential reflected XSS vector — not tested yet, saved for next session).

---

##  What Changed in the Vault

[Where we saved things. So you can find them later. Also: what we TRIED to change but you said no to.]

**New files created:**
- [`recon/instagram-oauth-flow-YYYY-MM-DD.md`] — Complete OAuth flow mapping

**Files modified:**
- [`bypass/04-oauth-bypass.md`] — Added Instagram-specific `state` stripping observation
- [`payloads/05-oauth-csrf.md`] — Added 3 new redirect_uri bypass payloads

**Proposed but rejected (by you):**
- [X] We wanted to add a new SSRF test for the redirect_uri, but you said no — save for next session

---

## ⚡ ONE Thing You Can Use Tomorrow

[Single most important takeaway. Actionable. If you only remember ONE thing — this is it.]

> When testing OAuth CSRF, if `state` is stripped but `redirect_uri` isn't, pivot to open redirect + token theft instead of CSRF. Don't waste time on a dead vector.

---

##  New Technique: You Can Do This Yourself

[A specific technique the human can execute WITHOUT the agent. The goal is YOU learn to do it, not just watch me do it.]

**Technique:** OAuth parameter differential fuzzing

**How YOU do it (without me):**
1. Capture the OAuth authorization request in Burp
2. Send to Intruder, set `redirect_uri` as the payload position
3. Use your `oauth-redirect-payloads.txt` wordlist (in `payloads/`)
4. Watch for: HTTP 302 to your URL, reflected URL in error page, `<script>` in error page
5. If `state` param acts differently than `redirect_uri` — note that. It's the key.

**Time to execute:** 10 minutes per OAuth endpoint
**Tool needed:** Burp Intruder + the payload list (already in vault)

---

##  Your Turn: Practice This Without Me

[A small challenge. The human tries this without the agent's help. Teaches independence.]

**Task:** Find ANY website that uses OAuth (Google Sign-In, Facebook Login, etc.). Capture the OAuth flow. Check: does the `state` parameter exist? Try removing it. What happens? Write down the behavior.

**Success:** You can describe what happens when `state` is missing or modified — without asking me.

**Time:** 15 minutes
**Share your result:** Write it in your daily note. Teach me what you found.

---

##  What We'll Do Next Session

[1-2 lines. What's on deck? Creates continuity.]

- Test the reflected URL in Instagram's error page for XSS (saved in recon notes)
- If XSS doesn't work, test the open redirect for token theft

---

## Human Confirmation

> [ ] I read this teach-back.
> [ ] I understand what we learned.
> [ ] I can do the "Your Turn" task without the agent.
> [ ] I'll apply the "One Thing" tomorrow.

---

*This file was auto-generated by your agent. You are not being replaced. You are being taught.*
